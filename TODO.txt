
<form class="nbPerPage" method="post" action="">
            <select>Affichage par page:
                <option value="25">25</option>
                <option value="50" style="border: 1px solid red;">50</option>
                <option value="75">75</option>
            </select>
        </form>
		

// Fonction quand on click sur le formulaire de sélection de nb elements par page :
$('.nbPerPage select').change(function(){
	// Change le nb d'éléments par page (normal...) + 
	// Recharge le contenu de la page avec le nouveau contenu limité à 20.
	// TODO : rajouter dans l'url le &pageNb=getNumPage()&cat=getCategorie()
	$.get("/societe-1.html?nbElem="+$('.nbPerPage select').val(), function(data) {
		// Changer le contenu de la page avec les nouvelles données recues (celles contenues dans data) 
}); //,'json' // à mettre après l'accolade
	})

// Lors du click sur une catégorie : 
// Meme principe, on fait un &.get en ajax, qui récupère sur la page les éléments/variables pageNb et catégorie et nbElemPerPage ... 
// Envoie le get avec l'url?var1=...&var2=...
// Met à jour le contenu de la page. (si data != null)


	
	
$.get("http://localhost/prog2/societe-1.html", function(data) {
	data.nbPerPage = 25; // A enelever
	$('.nbPerPage select option[value="'+data.nbPerPage+'"]').prop('selected', true);
}); //,'json' // à mettre après l'accolade

*************************************************** KOUKOU SAY FAIT ****************************************************

******************************CALCULER LE NOMBRE NOMBRE DE PAGE ET AFFICHER UNE PAGINATION******************************

Ligne 68 de vues/societe ne marche pas correctement
Il ne récupère pas le numéro de page correcte


************************************************************************************************************************
PAGINATION FINIE AIIIIIIIIIIIIIIIIIGHHHT
************************************************************************************************************************

Problème: clic categorie 1 -> nb par page, puis clic sur page 2 

********************************************************* PB FIXED *****************************************************

Ajouter les boutons de suppression d'objet de la fenetre qui s'ouvre lorsque l'on passe la souris sur "Mon panier"

Retravailler la méthode d'ajout d'un objet au panier?! Peut-être le faire en ajax ?

Modif quantité dans le panier, il faut modifier le prix + prix total PK IL Y A LE MINICART QUI NE SE MET PAS A JOUR APRES UN ACHAT?
La fonction minicart est include dans le header, du coup elle est réalisé avant la fonction add2Cart, il faudrait l'appeller dans le footer.

Il faut modifier l'ajout des articles pour le passer en Ajax afin de guarder la catégorie ouverte etc...